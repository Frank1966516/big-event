<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.CategoryMapper">
    <!-- 新增文章分类 -->
    <insert id="insert" parameterType="com.itheima.pojo.Category">
        insert into category
            (category_name, category_alias, create_user, create_time, update_time)
        values
            (#{categoryName}, #{categoryAlias}, #{createUser}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新文章分类 -->
    <update id="update" parameterType="com.itheima.pojo.Category">
        update category
        <set>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="categoryAlias != null and categoryAlias != ''">category_alias = #{categoryAlias},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>
</mapper>